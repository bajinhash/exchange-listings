{
  "name": "exchange-listings",
  "version": "1.1.0",
  "description": "Daily crypto exchange new-listing announcements scraper + AI formatter + static viewer (8 exchanges incl. HTX)",
  "scripts": {
    "scrape": "node scripts/scraper.js",
    "format": "node scripts/format.js",
    "basic-format": "node scripts/basic-format.js",
    "weekly": "node scripts/weekly.js",
    "daily": "npm run scrape && npm run basic-format && npm run weekly"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.88.0",
    "playwright": "^1.52.0"
  }
}
